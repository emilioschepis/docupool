export type PendingDocument = {
  id: string;
  user_id: string;
  title: string;
  description: string;
  file_key: string;
};

export type PublicDocument = {
  id: string;
  user_id: string;
  title: string;
  description: string;
  file_key: string;
};
